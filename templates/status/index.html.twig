{% extends 'base.html.twig' %}

{% block title %}Liste des Statuts{% endblock %}

{% block body %}
    <section class="container my-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3>Il y a {{ status | length }} statuts actuellement</h3>
            <a href="{{ path('status_create') }}" class="btn btn-primary">Créer un nouveau statut</a>
        </div>

        <div class="row">
            {% for statusItem in status %}
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card shadow-sm h-100">
                        <div class="card-body">
                            <h5 class="card-title">{{ statusItem.name }}</h5>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}
